// components/nurseIntake/PatientSummary.jsx
import React from "react";
import { Card, CardContent, CardHeader, CardTitle } from "../ui/card";
import { User, Activity, FileText, Pill } from "lucide-react";
import { motion } from "framer-motion";

export default function PatientSummary() {
  const patient = {
    name: "JAMES LEE",
    recordNumber: "SR-1234567",
    age: 30,
    sex: "M",
    dob: "2 January 1995",
    complaint: "Feeling more tired recently, sometimes light-headed in the afternoon.",
    vitals: {
      bloodPressure: "148/92 mmHg (↑)",
      heartRate: "86 bpm",
      spo2: "97% RA",
      temperature: "36.9 °C",
      height: "170 cm",
      weight: "82.5 kg (BMI 28.6)",
    },
    medicalHistory: {
      chronic: [
        "Type 2 Diabetes Mellitus (diagnosed 2014)",
        "Hypertension (diagnosed 2018)",
        "Hyperlipidaemia (diagnosed 2018)",
      ],
      surgery: ["Appendectomy (2010)"],
      allergies: ["Penicillin (rash reaction)"],
    },
    medications: "Not Applicable",
    labs: "Not Applicable",
  };

  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      transition={{ duration: 0.4 }}
      className="space-y-6"
    >
      {/* Patient Profile */}
      <Card className="shadow-sm border-0">
        <CardHeader>
          <CardTitle className="flex items-center gap-2 text-xl">
            <User className="w-5 h-5 text-blue-600" />
            {patient.name}
          </CardTitle>
        </CardHeader>
        <CardContent className="space-y-2">
          <p><strong>Medical Record Number:</strong> {patient.recordNumber}</p>
          <p><strong>Age/Sex:</strong> {patient.age}/{patient.sex}</p>
          <p><strong>DOB:</strong> {patient.dob}</p>
          <p><strong>Complaint:</strong> “{patient.complaint}”</p>
        </CardContent>
      </Card>

      {/* Vitals */}
      <Card className="shadow-sm border-0">
        <CardHeader>
          <CardTitle className="flex items-center gap-2 text-xl">
            <Activity className="w-5 h-5 text-emerald-600" />
            Vitals
          </CardTitle>
        </CardHeader>
        <CardContent className="grid grid-cols-1 md:grid-cols-2 gap-2">
          <p><strong>Blood Pressure:</strong> {patient.vitals.bloodPressure}</p>
          <p><strong>Heart Rate:</strong> {patient.vitals.heartRate}</p>
          <p><strong>SpO₂:</strong> {patient.vitals.spo2}</p>
          <p><strong>Temperature:</strong> {patient.vitals.temperature}</p>
          <p><strong>Height:</strong> {patient.vitals.height}</p>
          <p><strong>Weight:</strong> {patient.vitals.weight}</p>
        </CardContent>
      </Card>

      {/* Medical History */}
      <Card className="shadow-sm border-0">
        <CardHeader>
          <CardTitle className="flex items-center gap-2 text-xl">
            <FileText className="w-5 h-5 text-teal-600" />
            Medical History
          </CardTitle>
        </CardHeader>
        <CardContent className="space-y-2">
          <div>
            <strong>Chronic Conditions:</strong>
            <ul className="list-disc ml-6">
              {patient.medicalHistory.chronic.map((c, i) => (
                <li key={i}>{c}</li>
              ))}
            </ul>
          </div>
          <div>
            <strong>Past Surgical History:</strong>
            <ul className="list-disc ml-6">
              {patient.medicalHistory.surgery.map((s, i) => (
                <li key={i}>{s}</li>
              ))}
            </ul>
          </div>
          <div>
            <strong>Allergies:</strong>
            <ul className="list-disc ml-6">
              {patient.medicalHistory.allergies.map((a, i) => (
                <li key={i}>{a}</li>
              ))}
            </ul>
          </div>
        </CardContent>
      </Card>

      {/* Medications & Labs */}
      <Card className="shadow-sm border-0">
        <CardHeader>
          <CardTitle className="flex items-center gap-2 text-xl">
            <Pill className="w-5 h-5 text-purple-600" />
            Current Medications & Labs
          </CardTitle>
        </CardHeader>
        <CardContent className="space-y-2">
          <p><strong>Current Medications:</strong> {patient.medications}</p>
          <p><strong>Recent Labs:</strong> {patient.labs}</p>
        </CardContent>
      </Card>
    </motion.div>
  );
}
