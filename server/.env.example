#<rolename>: role which have the correct access to the db
#<password>: password of the role
#<db_name>: name of the database where the data locates
#port numbe can be changed
MONGO_URI=mongodb+srv://<rolename>:<password>@cluster0.k7asfxg.mongodb.net/<db_name>?retryWrites=true&w=majority
PORT=5001
NODE_ENV=development
# When true, uses an in-memory MongoDB for local dev (no external DB required)
USE_IN_MEMORY=false

# Auth
JWT_SECRET=change_this_in_production
JWT_EXPIRES_IN=7d

# Speech processing (disabled by default)
ENABLE_SPEECH=false
# Optional: set to enable speech transcription via Hugging Face and OpenAI
HUGGINGFACE_API_KEY=
HF_MEDICAL_WHISPER_MODEL=openai/whisper-base
# OPENAI_API_KEY is already used elsewhere in the project if you enable Whisper fallback
# OPENAI_MODEL=gpt-4

# CORS origin for the React app
CLIENT_ORIGIN=http://localhost:3000